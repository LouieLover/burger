<li>
	{{name}}

	<button class="change-burger" data-id="{{id}}" data-newdevoured="{{devoured}}">
		{{#if devoured}}EAT A BURGER!{{else}}DEVOURED!{{/if}}
	</button>
	<button class="delete-burger" data-id="{{id}}">
		Delete Burger
	</button>
	
</li>
